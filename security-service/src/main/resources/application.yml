server:
  port: 8030
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/security
    username: root
    password: 123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  application:
    name: SECURITY-SERVICE

  eureka:
    instance:
      prefer-Ip-Address: true

